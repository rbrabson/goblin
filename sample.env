# Discord Bot Configuration - Get these from Discord Developer Portal
DISCORD_BOT_TOKEN=your_bot_token_here
DISCORD_APP_ID=your_app_id_here

# Directory for configuration files (inside container)
DISCORD_CONFIG_DIR=/config

# Goblin theme - change only if using custom themes
DISCORD_BANK_FILE="clash"
DISCORD_BLACKJACK_THEME="clash"
DISCORD_GUILD_FILE="clash"
DISCORD_HEIST_FILE="clash"
DISCORD_PAYDAY_FILE="clash"
DISCORD_RACE_FILE="clash"
DISCORD_SLOTS_THEME="clash"

# Logging level: debug, info, warn, error, fatal
LOG_LEVEL=info

# Development server (optional) - uncomment and set for dev server only
# DISCORD_GUILD_ID=your_dev_server_id

# MongoDB Connection - Option 1: External MongoDB (e.g., MongoDB Atlas)
MONGODB_DATABASE=your_database_name_here
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# MongoDB Connection - Option 2: Local MongoDB Container Settings
# Use these when running MongoDB in Docker Compose
MONGODB_ROOT_USER=admin
MONGODB_ROOT_PASSWORD=your_secure_password_here
MONGODB_DATABASE=goblin

# For local MongoDB, the URI would be:
# MONGODB_URI=mongodb://admin:your_secure_password_here@mongodb:27017/goblin